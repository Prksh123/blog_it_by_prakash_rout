import React from "react";

import { Typography } from "@bigbinary/neetoui";

const PageTitle = ({ title }) => (
  <Typography className="mt-8 text-3xl font-semibold" style="h2">
    {title}
  </Typography>
);

export default PageTitle;
